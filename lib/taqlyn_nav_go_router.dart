/// Soft go_router helpers — hold a pending deep link until navigation is ready.
///
/// No Match / referrer / pasteboard logic. Pair with `taqlyn_sdk` ready-gate.
library;

export 'src/pending_deep_link.dart';
export 'src/deferred_nav.dart';
