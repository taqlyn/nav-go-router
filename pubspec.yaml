name: taqlyn_nav_go_router
description: Soft go_router ready-gate helpers for Taqlyn deferred deep links (no Match logic).
version: 0.1.1
homepage: https://taqlyn.com
repository: https://github.com/taqlyn/nav-go-router
issue_tracker: https://github.com/taqlyn/nav-go-router/issues

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
